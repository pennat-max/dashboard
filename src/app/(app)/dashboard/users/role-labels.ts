/** Shared labels for role selects (1–4). */
export const ROLE_LABELS: Record<number, string> = {
  1: "1 — General (partial KPI + main lists)",
  2: "2 — View all cards & charts (read-only)",
  3: "3 — Full access (edit)",
  4: "4 — Admin (manage users)",
};
